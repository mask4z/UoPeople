package Unit4.LearningJournal;


import Test.TextIO;

/**
 *
 */
public class SetCalculator {

    /*
    Create two TreeSets for two operands.

    Ask for input in the form of [n,a,b,...] + || - || * [n,a,b,...] to populate the two TreeSets.
    If syntax is incorrect, throw IllegalArgumentException.

    Calculate the union, intersection, or difference depending on the operator supplied.
     */
}